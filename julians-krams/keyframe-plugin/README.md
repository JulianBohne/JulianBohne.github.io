# Keyframe Plugin
This plugin can be used to create keyframes for in `iframe` in `reveal.js` (by creating fragments).

It is under construction currently and the API isn't set in stone at all, so use at your own risk :D
Expect some bugs...
